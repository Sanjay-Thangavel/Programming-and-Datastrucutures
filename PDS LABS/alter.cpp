#include <bits/stdc++.h>



void function(int arr,int n)
{
    for(int i=0;i<n;i++)
    {
        if ( (i%2==0) && (arr[i] <=-1) )
        {
           continue;
        }
        else if  ( (i%2!=0) && (arr[i]>0) )
        {
           continue;
        }
        else
        {
            if()
        }
    }


}
using namespace std;

int main(){
    int n;
   cout<<"entre n :"<<endl;
   cin>>n;

   int arr[n];

   for(int i=0; i<n; i++)
   {
       cout<<"entre "<<i+1<<endl;
       cin>>arr[i];
   }

   void function(int arr, int n);


}
